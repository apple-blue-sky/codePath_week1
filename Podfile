# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'codePath_week1' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for codePath_week1
  pod "AlamofireImage"
  

  target 'codePath_week1Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'codePath_week1UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
